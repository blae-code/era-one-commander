Shared conventions: every function reads entities as service role via createClientFromRequest; modifier stacking is
additive within a stat (sum of Add/Subtract fractions, then Multiply/Set), see engagement/entry.ts `applyMods`.
