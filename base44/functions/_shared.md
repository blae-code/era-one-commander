Backend functions for the game dataset. See ../GAME-DATA-CONTRACT.md §3. All use `npm:@base44/sdk`'s
`createClientFromRequest` and read entities as service role. They require the data to be imported
(`/gamedata`) first.
